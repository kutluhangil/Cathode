/**
 * Kabuk yerleşim sabitleri — tek kaynak.
 * windowsStore (maximize sınırları) ve dock/SystemBar bileşenleri kullanır.
 */

/** Üst sistem çubuğu yüksekliği (px) */
export const SYSTEM_BAR_H = 28;

/** Dock için ekran altında ayrılan alan (px) */
export const DOCK_RESERVE = 84;

/** Boot "oturum başına bir kez" anahtarı (sessionStorage) */
export const BOOT_KEY = "cathode.booted";
