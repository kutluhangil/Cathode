# TR/EN Dil Desteği (i18n) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Siteye canlı TR/ENG dil değişimi eklemek; tüm görünür UI metinleri iki dilde.

**Architecture:** Hafif özel i18n — Zustand `settingsStore`'a `lang` alanı, `src/lib/i18n/` altında sözlükler (`tr`/`en`) ve `useT()` hook'u. Her component sabit stringleri sözlük anahtarıyla `useT()` üzerinden render eder. Route yok, tek sayfa, anlık geçiş.

**Tech Stack:** Next.js 14 (App Router), TypeScript strict, Zustand persist. Yeni bağımlılık YOK.

## Global Constraints
- ASLA `git commit` / `git push` / branch oluşturma — Kutluhan elle yapar. Plandaki "Commit" adımları kullanıcı içindir; agent commit atmaz, sadece "commit'e hazır" der.
- Yeni bağımlılık eklenmez.
- TypeScript strict; kod tipli.
- Doğrulama test framework'ü YOK → her task sonu: `npx tsc --noEmit` temiz + gerektiğinde `npm run lint`. Bütünsel doğrulama (Task son) `npm run build` + tarayıcı kontrolü.
- Hata yutma yok: eksik çeviri anahtarı sessizce boş dönmez, anahtarın kendisi döner (görünür kalır).
- Emüle OS iç arayüzü ve gerçek ürün adları (KolibriOS, FreeDOS…) çevrilmez.

---

## Dosya Yapısı
- Create: `src/lib/i18n/types.ts` — `Lang`, `Dict`, `TParams` tipleri.
- Create: `src/lib/i18n/tr.ts` — Türkçe sözlük (kaynak dil, kanonik anahtar ağacı).
- Create: `src/lib/i18n/en.ts` — İngilizce sözlük (`tr` ile birebir anahtar).
- Create: `src/lib/i18n/index.ts` — `dictionaries` map + `LANGS` meta.
- Create: `src/lib/i18n/useT.ts` — `useT()` hook, `t(key, params?)`.
- Modify: `src/store/settingsStore.ts` — `lang` + `setLang`, persist v2→v3 migrate.
- Modify: `src/components/ThemeProvider.tsx` — `document.documentElement.lang` senkronu.
- Modify: dil seçici → `src/components/apps/Settings.tsx`, `src/components/dock/SystemBar.tsx`.
- Modify (string taşıma): aşağıdaki task'larda listelenen tüm component/data dosyaları.

---

## Task 1: i18n çekirdeği + store `lang`

**Files:**
- Create: `src/lib/i18n/types.ts`, `src/lib/i18n/tr.ts`, `src/lib/i18n/en.ts`, `src/lib/i18n/index.ts`, `src/lib/i18n/useT.ts`
- Modify: `src/store/settingsStore.ts`

**Interfaces:**
- Produces:
  - `type Lang = "tr" | "en"`
  - `useT(): (key: string, params?: Record<string, string | number>) => string`
  - `useSettings` state: `lang: Lang`, `setLang: (l: Lang) => void`
  - `dictionaries: Record<Lang, Dict>`, `LANGS: { id: Lang; label: string }[]`

- [ ] **Step 1: `types.ts`**

```ts
export type Lang = "tr" | "en";

/** Nested string tablosu — yaprak değer string ya da interpolasyonlu string. */
export type Dict = { [key: string]: string | Dict };

export type TParams = Record<string, string | number>;
```

- [ ] **Step 2: `tr.ts` ve `en.ts` iskeleti**

Namespace'ler: `common, menu, dock, startMenu, systemBar, settings, about, terminal, notepad, systems, games, emulator, commandPalette, windows, boot, screensaver, installHint`.

Task 1'de yalnız `common` + `settings.language*` + `systemBar` anahtarlarını doldur (seçiciler Task 2'de bunları kullanacak). Diğer namespace'ler sonraki task'larda büyür.

`tr.ts`:
```ts
import type { Dict } from "./types";

export const tr = {
  common: {
    close: "kapat",
    minimize: "küçült",
    maximize: "büyült",
    on: "açık",
    off: "kapalı",
  },
  systemBar: {
    language: "dil",
  },
  settings: {
    language: "Dil",
    languageTr: "Türkçe",
    languageEn: "İngilizce",
  },
} satisfies Dict;
```

`en.ts` (birebir anahtar, İngilizce değer):
```ts
import type { Dict } from "./types";

export const en = {
  common: {
    close: "close",
    minimize: "minimize",
    maximize: "maximize",
    on: "on",
    off: "off",
  },
  systemBar: {
    language: "language",
  },
  settings: {
    language: "Language",
    languageTr: "Turkish",
    languageEn: "English",
  },
} satisfies Dict;
```

- [ ] **Step 3: `index.ts`**

```ts
import type { Lang } from "./types";
import { tr } from "./tr";
import { en } from "./en";

export const dictionaries = { tr, en } as const;

export const LANGS: { id: Lang; label: string }[] = [
  { id: "tr", label: "TR" },
  { id: "en", label: "EN" },
];

export type { Lang } from "./types";
```

- [ ] **Step 4: `useT.ts`**

```ts
"use client";

import { useCallback } from "react";
import { useSettings } from "@/store/settingsStore";
import { dictionaries } from "./index";
import type { Dict, TParams } from "./types";

function resolve(dict: Dict, key: string): string | undefined {
  const val = key.split(".").reduce<string | Dict | undefined>(
    (acc, part) => (acc && typeof acc === "object" ? acc[part] : undefined),
    dict,
  );
  return typeof val === "string" ? val : undefined;
}

function interpolate(tpl: string, params?: TParams): string {
  if (!params) return tpl;
  return tpl.replace(/\{(\w+)\}/g, (_, k) =>
    k in params ? String(params[k]) : `{${k}}`,
  );
}

/** Aktif dile göre çeviri fonksiyonu döndürür. Anahtar yoksa anahtarı döner. */
export function useT() {
  const lang = useSettings((s) => s.lang);
  return useCallback(
    (key: string, params?: TParams) => {
      const hit = resolve(dictionaries[lang], key);
      return hit === undefined ? key : interpolate(hit, params);
    },
    [lang],
  );
}
```

- [ ] **Step 5: `settingsStore.ts` — `lang` ekle**

`SettingsState` interface'ine ekle:
```ts
import type { Lang } from "@/lib/i18n/types";
// ...
  lang: Lang;
  setLang: (l: Lang) => void;
```
Varsayılan + setter (store gövdesine):
```ts
      lang: "tr",
      setLang: (lang) => set({ lang }),
```
Persist `version: 2` → `3`, migrate içine ekle:
```ts
        if (s.lang !== "tr" && s.lang !== "en") s.lang = "tr";
```

- [ ] **Step 6: Doğrula**

Run: `npx tsc --noEmit`
Expected: hata yok.

- [ ] **Step 7: Commit (kullanıcı)** — "commit'e hazır: i18n çekirdeği + store lang".

---

## Task 2: Dil seçiciler (Settings + SystemBar)

**Files:**
- Modify: `src/components/apps/Settings.tsx`, `src/components/dock/SystemBar.tsx`

**Interfaces:**
- Consumes: `useSettings().lang / setLang`, `useT`, `LANGS`, `Segmented` (varsa).

- [ ] **Step 1: SystemBar toggle**

`SystemBar.tsx` içine, mevcut sağ blok düzenine uygun kompakt `TR | EN` kontrolü ekle. Desen (mevcut buton stiliyle uyumlu hale getir):
```tsx
const lang = useSettings((s) => s.lang);
const setLang = useSettings((s) => s.setLang);
// ...
<button
  onClick={() => setLang(lang === "tr" ? "en" : "tr")}
  className="font-mono text-[11px] text-text-dim transition-colors hover:text-accent"
  title={t("systemBar.language")}
>
  {lang === "tr" ? "TR" : "EN"}
</button>
```
Not: SystemBar mevcut buton/aralık desenini birebir kullan; yeni stil uydurma.

- [ ] **Step 2: Settings dil kontrolü**

`Settings.tsx` içinde mevcut ayar satırı desenine (accent/crt satırları) uygun bir "Dil" satırı ekle. Mevcut `Segmented` bileşeni varsa onu kullan:
```tsx
<Segmented
  value={s.lang}
  onChange={(v) => s.setLang(v as Lang)}
  options={[
    { value: "tr", label: t("settings.languageTr") },
    { value: "en", label: t("settings.languageEn") },
  ]}
/>
```
`Segmented` API'si farklıysa mevcut kullanımına uydur (dosyadaki başka `Segmented` çağrısına bak).

- [ ] **Step 3: Doğrula**

Run: `npx tsc --noEmit` → temiz.
Tarayıcı: SystemBar `TR/EN` toggle ve Settings segmenti `lang`'ı değiştiriyor; yenilemede korunuyor (localStorage `cathode.settings`).

- [ ] **Step 4: Commit (kullanıcı)** — "commit'e hazır: dil seçiciler".

---

## Task 3: ThemeProvider `<html lang>` senkronu

**Files:** Modify: `src/components/ThemeProvider.tsx`

- [ ] **Step 1:** `lang` değişiminde `document.documentElement.lang` güncelle.

```tsx
const lang = useSettings((s) => s.lang);
useEffect(() => {
  document.documentElement.lang = lang;
}, [lang]);
```
(ThemeProvider zaten client + `useSettings` kullanıyorsa mevcut effect desenine ekle.)

- [ ] **Step 2:** `npx tsc --noEmit` temiz. Commit (kullanıcı).

---

## Task 4–12: String taşıma (her task = bir alan)

Her taşıma task'ı **aynı deseni** izler:

1. Dosyadaki tüm görünür sabit TR string'leri bul (JSX text, `title`, `placeholder`, `aria-label`, buton etiketi, `label` alanları). Kod/teknik string (className, id, url) DEĞİL.
2. İlgili namespace'e anahtar ekle: `tr.ts`'e TR değeri (kaynaktan aynen), `en.ts`'e İngilizce çevirisi. Anahtar ağacı iki dosyada birebir aynı.
3. Component'te `const t = useT();` ekle (yoksa) ve string'i `t("namespace.key")` ile değiştir. Dinamik metin → interpolasyon: `t("emulator.progress", { n: progress })`.
4. `npx tsc --noEmit` temiz.
5. Commit (kullanıcı).

Doğrulama (her task): `tsc` temiz + o alanı tarayıcıda TR/EN'de gözle kontrol — sabit metin kalmadı, iki dil de doğru.

### Task 4: Masaüstü kabuğu → namespace `menu`, `dock`, `startMenu`, `screensaver`
- Files: `src/components/desktop/Desktop.tsx` (sağ tık menü etiketleri: "Yenile", "Duvar kâğıdını değiştir", "Ayarlar", "Hakkında"; disclaimer metni), `src/components/desktop/Screensaver.tsx`, `src/components/dock/Dock.tsx`, `src/components/dock/StartMenu.tsx`, `src/components/dock/Clock.tsx`.
- Not: `Desktop.tsx` disclaimer uzun cümle → tek anahtar `menu.disclaimer`.

### Task 5: Pencere sistemi → namespace `windows`, `commandPalette`
- Files: `src/components/window/TitleBar.tsx` (kontrol `title`/`aria-label`), `src/components/window/WindowSwitcher.tsx`, `src/components/window/WindowManager.tsx`, `src/components/desktop/CommandPalette.tsx` (placeholder, ipuçları, boş durum).

### Task 6: Boot + InstallHint → namespace `boot`, `installHint`
- Files: `src/components/boot/BootScreen.tsx`, `src/components/InstallHint.tsx`.

### Task 7: Settings uygulaması → namespace `settings`
- Files: `src/components/apps/Settings.tsx` (tüm etiket/başlık/açıklamalar; ekran koruyucu bölümü dahil). Task 2'de eklenen `settings.language*` anahtarlarını genişlet.

### Task 8: About → namespace `about`
- Files: `src/components/apps/About.tsx`.

### Task 9: Terminal → namespace `terminal`
- Files: `src/components/apps/Terminal.tsx` (komut çıktıları, yardım metni, prompt etiketleri, hata mesajları). Komut adlarının kendisi (help, clear vb.) çevrilmez; açıklamaları çevrilir.

### Task 10: Notepad → namespace `notepad`
- Files: `src/components/apps/Notepad.tsx` (placeholder, durum çubuğu, buton etiketleri).

### Task 11: Systems + Emülatör → namespace `systems`, `emulator`
- Files: `src/components/apps/Systems.tsx`, `src/components/apps/emulator/EmulatorWindow.tsx` (kontrol butonları "sıfırla/uyut/indir/tam ekran", HINTS "imaj indiriliyor…"/"sistem başlatılıyor…", "kayıtlı oturum var/devam et/sil", "tıkla = fareyi yakala · Esc = ekranı bırak", hata metinleri), `src/components/apps/emulator/LazyEmulator.tsx`, `src/components/apps/emulator/JsDosScreen.tsx`, `src/components/apps/emulator/LazyJsDos.tsx`.
- Dinamik: indirme ilerlemesi/yüzde interpolasyonla.

### Task 12: Games + data katmanı → namespace `games`
- Files: `src/components/apps/Games.tsx`, ve `src/data/apps.ts`, `src/data/games.ts`, `src/data/os.ts`, `src/data/wallpapers.ts` içindeki **kullanıcıya görünen** `name`/`label`/`description`/`hint` alanları.
- data desen kararı: görünen alanı `{ tr: string; en: string }` yap ve tüketen component'te `field[lang]` ile oku; ya da alanı i18n anahtarına çevir. Dosyadaki mevcut tip yapısına en az dokunacak yolu seç, uygulayınca tipleri güncelle. Gerçek OS/oyun adları (KolibriOS, Snake…) çevrilmez — sadece açıklama/etiket metinleri.

---

## Task 13: Bütünsel doğrulama

**Files:** (yok — sadece doğrulama + kalan sweep)

- [ ] **Step 1: Kaçan string taraması**

Run:
```bash
cd /Volumes/ProjectVault/Cathode
grep -rnE "[çğıöşüÇĞİÖŞÜ]" src/components src/data --include=*.tsx --include=*.ts | grep -v "useT\|// \|/\*\|i18n/" 
```
Çıkan her satır: ya çevrilmemiş görünür metin (→ ilgili namespace'e taşı) ya da yorum/teknik (yok say). Kalan görünür metni bitir.

- [ ] **Step 2: Sözlük denklik kontrolü**

`tr.ts` ve `en.ts` anahtar ağaçları birebir aynı mı gözle/lint kontrol et. Eksik anahtar → tamamla.

- [ ] **Step 3: Build + lint**

Run: `npx tsc --noEmit && npm run lint && npm run build`
Expected: üçü de temiz.

- [ ] **Step 4: Tarayıcı kabul**

TR ve EN'de tüm ekranları gez: masaüstü, menü, dock, pencere başlıkları, komut paleti, Ayarlar, Hakkında, Terminal, Notepad, Systems/emülatör, Games. Sabit çevrilmemiş metin kalmamalı; dil anlık değişmeli; yenilemede korunmalı.

- [ ] **Step 5: Commit (kullanıcı)** — "commit'e hazır: i18n bütünsel doğrulama".

---

## Self-Review Notları
- Spec kapsamı: tüm namespace'ler task'lara dağıtıldı (Task 4–12), data katmanı Task 12, doğrulama Task 13. Kapsam tam.
- DoD eşleşmesi: anlık geçiş (useT lang'a bağlı), persist (store v3), seçiciler (Task 2), eksik metin taraması (Task 13 Step 1), build temiz (Task 13 Step 3).
- Interpolasyon tek yerde (`useT`), tüm dinamik metin oradan geçer — tutarlı.
